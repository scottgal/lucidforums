import { EasyMDE, IEasyMDEPlugin } from "../easymde";
import { IToolbarButtonOptions } from "./default-toolbar";
export declare class Toolbar implements IEasyMDEPlugin {
    #private;
    private editor;
    element: HTMLDivElement;
    constructor(editor: EasyMDE, toolbarLayout: IToolbarButtonOptions[][]);
    build(toolbarLayout: IToolbarButtonOptions[][]): Promise<void>;
    destroy(): Promise<void>;
}
