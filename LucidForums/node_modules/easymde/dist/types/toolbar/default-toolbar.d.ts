import { ViewUpdate } from "@codemirror/view";
import { EasyMDE } from "../easymde";
export interface IToolbarButtonOptions {
    action?: any;
    active?: boolean | ((editor: EasyMDE, update: ViewUpdate) => boolean) | ((editor: EasyMDE, update: ViewUpdate) => Promise<boolean>);
    icon: string;
    readonly name: string;
    title: string;
}
export declare const defaultToolbar: IToolbarButtonOptions[][];
