import { EditorState } from "@codemirror/state";
import { EditorView } from "@codemirror/view";
/**
 * Checks whether the selection matches a formatted block of text.
 */
export declare const checkBlock: (editor: EditorView, characters: string, minimal?: boolean) => RegExpExecArray | null;
/**
 * Toggles a block of text to be formatted.
 */
export declare const toggleBlock: (editor: EditorView, characters: string) => void;
/**
 * Attempts to expand the cursor selection to the nearest logical block of text needs to be formatted.
 */
export declare const getExpandedSelection: (state: EditorState, characters: string, minimal?: boolean) => {
    from: number;
    to: number;
};
